import React from 'react'

export default function HowItWorksView() {
    return <div>
        <h1 className="text-right">/ How it works</h1>
        <p>
            Reflector oracle contracts are controlled by the multisig-protected consensus of reputable organizations.
            The smart contract admin account always has all node public keys as co-signers with a >50% multisig
            threshold, so more than half of the oracle backing nodes have to agree on a transaction in order to store
            price feed data or modify contract state.
        </p>
        <p>
            Each node independently calculates values of quoted prices using deterministic idempotent algorithms to
            ensure consistency, generates an update transaction, signs it with node's private key, and then shares it to
            other peers via WebSocket protocol. If for some reason (ledger access delay, failing connection, version
            incompatibility, adversary attack) any given node quotes a token/asset price different from other nodes, the
            transaction hash will not match the hash generated by the majority and such transaction will be discarded by
            the ledger. This way Reflector utilizes Stellar protocol underlying security to implement an uncomplicated
            yet robust consensus, which guarantees reliability, fault tolerance and regular price feed updates.
        </p>
        <p>
            For on-chain Stellar assets price feed data retrieval Reflector relies on a quorum of nodes connected to
            Stellar RPC nodes. Each node independently fetches trades and state information from the Stellar ledger.
            Price feeds for generic tokens get updated in a similar fashion, but nodes have to agree on the data pulled
            from external sources (CEX/DEX API, banks, forex venues, price aggregators, stock exchanges, derivative
            platforms, etc.) All historical price feed data is stored in the oracle contract and becomes immutable once
            it is written to the contract storage. Records are held in temporary storage subject to the contract&apos;s
            history retention period, so older entries can be evicted over time.
        </p>
        <p>
            Other contracts interact with oracle contracts, retrieving data stored earlier by Reflector consensus.
            Consumers can fetch historical ranges or simply pull the most recent token price depending on the use-case.            
            Check the <a href="interface">usage section</a> for details on available oracle functions and{' '}
            <a href="examples">examples</a> for practical guidance.
        </p>
        <div className="space">
            <img src="/images/reflector-diagram.svg" alt="Reflector consensus diagram"/>
        </div>
        <h3>Components</h3>
        <ul className="list">
            {components.map(component => <li key={component.title}>
                {component.link ?
                    <a href={component.link} target="_blank" rel="noopener">{component.title}</a> :
                    <div>{component.title}</div>
                }
                <div className="text-small dimmed">{component.description}</div>
            </li>)}
        </ul>
    </div>
}

const components = [
    {
        title: 'Reflector node',
        link: 'https://github.com/reflector-network/reflector-node',
        description: 'Fetches prices, maintains p2p communication with other nodes, and publishes price updates signed by the quorum'
    },
    {
        title: 'Oracle smart contract',
        link: 'https://github.com/reflector-network/reflector-contract',
        description: 'Contract implementation that incorporates consumer-facing interface with admin functionality'
    },
    {
        title: 'Contract JavaScript bindings',
        link: 'https://github.com/reflector-network/oracle-client',
        description: 'Provides convenient JS wrappers for interaction with the contract from JS runtime'
    },
    {
        title: 'Stellar Core DB connector',
        link: 'https://github.com/reflector-network/reflector-db-connector',
        description: 'Database connector library responsible for retrieving and parsing data from Stellar Core'
    },
    {
        title: 'Admin dashboard',
        link: 'https://github.com/reflector-network/admin-dashboard',
        description: 'Web interface for Reflector node operators, toolkit for managing Reflector cluster and orchestrating updates'
    },
    {
        title: 'Node API emulator',
        description: 'Utility for testing admin dashboard with various emulated node/cluster states'
    },
    {
        title: 'Stellar trades emulator',
        description: 'Simple trading bot for generating relevant trades activity on Testnet and Futurenet'
    }
]
